# online-order-program
This project is a command line interface (CLI) application designed to simulate a digital restaurant ordering process. It utilizes Python’s core features specifically dictionaries, while loops, and conditional logic to allow users to view a menu, select multiple items, and receive an automatically calculated total bill.
